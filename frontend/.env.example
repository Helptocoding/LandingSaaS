# Google Analytics 4 — Measurement ID.
# Prefijo PUBLIC_ obligatorio: Astro solo expone al cliente las variables PUBLIC_*.
# Si se deja vacía o se omite, Analytics no se carga y el sitio funciona igual.
# Copia este archivo a `.env` y coloca tu ID real:
PUBLIC_GA_MEASUREMENT_ID=G-6MTZLJ9ZN0
